/// Type-safe and error'd wrappers around the Twitter API. This module creates methods that perform
/// the acions we want in Twitter's API without in a manner that's more safe than writing raw
/// requests to the API. 


